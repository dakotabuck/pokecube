<p>
  <strong>Name:</strong>
  <%= @cube.name %>
</p>

<% if @cube.description %>
  <p>
    <strong>Description:</strong>
    <%= @cube.description %>
  </p>
<% end %>

<p>This cube
  <% if @cube.private == true %>
    <strong>is</strong>
  <% elsif @cube.private == false %>
    <strong>is not</strong>
  <% end %>
  private.
</p>

<p>
  Add a Card:
  <%= form_with(model: [ @cube, @cube.cubecards.build ], local: true) do |form| %>
  <p>
    <%=
      # This will eventually be replaced by a <select><option> each with the
      #   name of the card, the set, and the number with values equal to ID
      #   for easy insertion into cube_cards table.

      # Will also consider adding a coffee script that loads the image asset
      #   for each card upon selection (by Option.value)
      number_field(:card, :id, in: 1.0..8000.0, step: 1)
    %>
    <%= hidden_field_tag :id, @cube.id %>
  </p>
  <p>
    <%= form.submit %>
  </p>
  <% end %>
</p>

<%= link_to 'All cubes you own', controller: 'cubes' %>
